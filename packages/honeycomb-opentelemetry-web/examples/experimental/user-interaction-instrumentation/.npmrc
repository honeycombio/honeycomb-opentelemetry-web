ignore-scripts=true
# Quarantine newly published versions. Replaces the `before=2025-09-08`
# stopgap from #608; requires npm >= 11.10.0. Do not re-add `before` -- within
# a single source it overrides this.
min-release-age=7
